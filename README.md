<div align="center"> 
<img src="res/icon.png" width="40%"> 
</div>

# Ultimate-asm-compiler  
Универсальный компиятор ассеблера для risk-v совместимых процессоров, основанный на префабах которые описывают основные парраметры процессора:
Разрядность,Регистровый файл(мнемоники),ISA,Длинну машинного слова,Порядок байт,кодирование инструкций
Предостовляя быстрое создание aссемблера
# Описание основного языка 
### Синтаксис
Строка = инструкция + возможный комментарий не учитывая табуляцию. Аргументы разделяются через пробел
```asembly
;{ op } { arg1 } { arg2 } ...... ; коментарий
jmp _MAIN
    jmp _MAIN
        jmp _MAIN ; start

```
При возможности не оставляйте пустые строки, либо не используйте табуляции
``\t\t`` - плохо
---
### Представления чисел
Функция get_number(str) принимает числовой аргумент, возврашая число 

| **префикс** | **описание**          |    **особенности**     |
|:-------:|-------------------|:------------------:|
|         | целое десятичное  |         -          |
|   0x    | шестнадцатиричное |         -          |
|   0b    | двоичное          |         -          |
|   0o    | восьмиричное      |         -          |
|  ", '   | символ ASCII      | только один символ |
|    :    | метка             |         -          |

--- 
### Функции предпросессора
`#define` `Метка` `значение` ручное добавление метки 

``fn:`` ``метка``  метка подпрограммы

`:` `Адресс` разметка памяти

`data=` `Метка` `val,val,val .....` до 16 шт

``` asembly
#define _RAM 0x0000
#define .data 0x8000 ;EEPROM BANK0
#define .code 0x8F00 ;EEPROM BANK1 (4KB)

: :.data
data= hello_msng "H","E","L","L","O",0x00

: :.code
fn: main
(read) (hello_msg); ПРИМЕР!
```

```bin
0x0000: 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 ; ( _RAM )
                             .
                             .
0x8000: "H "E "L "L "O 00 00 00 00 00 00 00 00 00 00 00 ; .data
                             .
                             .
0x8F00: FA АА 80 00 00 00 00 00 00 00 00 00 00 00 00 00 00 ; .code (main)
       |read |adr16|
```
---
# Описание процессора
Процессоры хранятся в поддиректории `processors` и являются обычными модулями `.py` основа которого класс `parram`. 
```python
class parram:
    def __init__(self):
        self.mem_len = 0xFFFF 
        self.max_number = 0xFF
        self.width = 8 
        self.insruction_len = 4 
        self.order = 'big-endian'
        self.REGISTERS = {}
        self.COMMANDS = {
            'DATA=': 'spec',    
            '#define': 'spec', 
            'fn:': 'spec',      
            ':': 'spec'      
        }
        self.LEN = {
            "DATA=" : 1,        
            "#define" : 1,      
            "fn:" : 1,        
            ":" : 1 
        }
        self.functions = {}
        self.Labels = {}

```
---
# Парраметры `__init__`
## Числовые
| парраметр             | описание                            |
|-----------------------|-------------------------------------|
| `self.mem_len`        | Адресное пространство(слов)         |
| `self.max_number`     | Максимальное обрабатываемое число   |
| `self.width`          | Разрядность слова                   |
| `self.insruction_len` | Минимальная длинна инструкции(слов) |
---
## Cтроковые
| парраметр       | описание                                      |
|-----------------|-----------------------------------------------|
| `self.order`    | Порядок Слов `little-endian` или `big-endian` |
---
## Словари
### `self.REGISTERS` Регистровый файл 
 ```python
#Регистр : Индекс
REGISTERS = {
    "r0" : 0,
    "r1" : 1,
}

 ```
Считается что в каждый регистр шириной ровно в одно слово используеться функцией `get_register(name)` возвращает индекс или `REGISTER_INVALID`

---
### `self.COMMANDS` ISA
Показывает как кодируются инструкции парой Инструкция : eval формула

### Доступные функции:
#### Стандартные:
`int` `list` `range` `len` `sum` `sorted` `min` `max` `str` `abs` `round` `bool` `float` `hex` `bin` `ord` `chr` `List` `Tuple` `Dict` `Set`
#### Компилятор
| func          | return                        |
|---------------|-------------------------------|
| get_label     | метка                         |
| number_string | [число](#Представления-чисел) |
| get_register  | регистр                       |
А также [пользовательские](#Пользовательские-функции)
#### Переменные
|             |                      |
|-------------|----------------------|
| `arg1` - `arg4` | аргументы инструкции |

```python
COMMANDS = {
    'jmp' : "0xFF + get_label(arg1) << 32 ", # ПРИМЕР
}
```
---
### `self.LEN` Длинна инструкций
Количество машинных слов на инструкцию                             
```python
LEN = {            # Обязательно
    "DATA=" : 1,        # НЕ УДАЛЯТЬ
}
```
---
### `self.Labels` Начальные метки
можно оставить пустым или
```python
labels = {
    'INTERRUPT' : 0xFF00
}
```
---
### `self.functions`Пользовательские функции
Пользовательские функции указанные далее
```python
class parram():
    def __init__():
#   ....
        functions = {
            #TODO: здесь
            'RS0' : self.RS0, 
        }
#   ....
    def RS0(self, num):
        return num << 8
```
---


